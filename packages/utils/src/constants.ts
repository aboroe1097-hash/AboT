// Router classification thresholds
export const ROUTER_PHASE1_THRESHOLD = 0.75;
export const ROUTER_AMBIGUITY_MARGIN = 0.15;

// Context and file limits
export const MAX_CONTEXT_FILES = 20;
export const MAX_WORKSPACE_ENTRIES = 300;

// Timeout limits (in milliseconds)
export const MIN_TIMEOUT_MS = 1000;
export const MAX_TIMEOUT_MS = 600000;
export const DEFAULT_EXECUTION_TIMEOUT_MS = 120000;
export const DEFAULT_COMMAND_TIMEOUT_MS = 30000;

// API limits
export const MAX_TASK_LENGTH = 10000;
export const MAX_ROUTE_LIMIT = 200;
export const MAX_CHAT_MESSAGE_LIMIT = 300;
